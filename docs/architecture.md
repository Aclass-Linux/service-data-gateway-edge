# DataGatewayEdge Architecture

## Layers

- `core`: shared primitives and utilities
- `protocol`: packet and frame processing
- `data`: repository and connection abstractions
- `connectors`: external system adapters
- `hub`: orchestration and business flow
- `app`: executable entrypoint
