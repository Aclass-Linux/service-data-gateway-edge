#pragma once

#if defined(_WIN32) || defined(__CYGWIN__)
#  if defined(@EXPORT_MACRO@_EXPORTS)
#    define @EXPORT_MACRO@ __declspec(dllexport)
#  else
#    define @EXPORT_MACRO@ __declspec(dllimport)
#  endif
#else
#  define @EXPORT_MACRO@
#endif

